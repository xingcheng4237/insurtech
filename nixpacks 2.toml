[phases.setup]
nixPkgs = ["nodejs-22_x", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm lint", "pnpm check", "pnpm test", "pnpm audit --prod --audit-level=high", "pnpm build"]

[start]
cmd = "node dist/index.js"
